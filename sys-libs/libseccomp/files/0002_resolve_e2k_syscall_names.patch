diff '--color=auto' -ruaN libseccomp-2.5.5/include/seccomp-syscalls.h my_libseccomp-2.5.5/include/seccomp-syscalls.h
--- libseccomp-2.5.5/include/seccomp-syscalls.h	2025-05-15 12:52:21.027256107 +0300
+++ my_libseccomp-2.5.5/include/seccomp-syscalls.h	2025-05-15 12:47:58.183046082 +0300
@@ -2389,3 +2389,117 @@
 #define __SNR_write			__NR_write
 
 #define __SNR_writev			__NR_writev
+#define __PNR_pread -(20000+180)
+#ifdef __NR_pread
+#define __SNR_pread __NR_pread
+#else
+#define __SNR_pread __PNR_pread
+#endif
+#define __PNR_pwrite -(20000+181)
+#ifdef __NR_pwrite
+#define __SNR_pwrite __NR_pwrite
+#else
+#define __SNR_pwrite __PNR_pwrite
+#endif
+#define __PNR_macctl -(20000+223)
+#ifdef __NR_macctl
+#define __SNR_macctl __NR_macctl
+#else
+#define __SNR_macctl __PNR_macctl
+#endif
+#define __PNR_e2k_sigsetjmp -(20000+226)
+#ifdef __NR_e2k_sigsetjmp
+#define __SNR_e2k_sigsetjmp __NR_e2k_sigsetjmp
+#else
+#define __SNR_e2k_sigsetjmp __PNR_e2k_sigsetjmp
+#endif
+#define __PNR_e2k_longjmp -(20000+227)
+#ifdef __NR_e2k_longjmp
+#define __SNR_e2k_longjmp __NR_e2k_longjmp
+#else
+#define __SNR_e2k_longjmp __PNR_e2k_longjmp
+#endif
+#define __PNR_e2k_syswork -(20000+228)
+#ifdef __NR_e2k_syswork
+#define __SNR_e2k_syswork __NR_e2k_syswork
+#else
+#define __SNR_e2k_syswork __PNR_e2k_syswork
+#endif
+#define __PNR_e2k_longjmp2 -(20000+230)
+#ifdef __NR_e2k_longjmp2
+#define __SNR_e2k_longjmp2 __NR_e2k_longjmp2
+#else
+#define __SNR_e2k_longjmp2 __PNR_e2k_longjmp2
+#endif
+#define __PNR_set_backtrace -(20000+252)
+#ifdef __NR_set_backtrace
+#define __SNR_set_backtrace __NR_set_backtrace
+#else
+#define __SNR_set_backtrace __PNR_set_backtrace
+#endif
+#define __PNR_get_backtrace -(20000+253)
+#ifdef __NR_get_backtrace
+#define __SNR_get_backtrace __NR_get_backtrace
+#else
+#define __SNR_get_backtrace __PNR_get_backtrace
+#endif
+#define __PNR_access_hw_stacks -(20000+254)
+#ifdef __NR_access_hw_stacks
+#define __SNR_access_hw_stacks __NR_access_hw_stacks
+#else
+#define __SNR_access_hw_stacks __PNR_access_hw_stacks
+#endif
+#define __PNR_setcontext -(20000+369)
+#ifdef __NR_setcontext
+#define __SNR_setcontext __NR_setcontext
+#else
+#define __SNR_setcontext __PNR_setcontext
+#endif
+#define __PNR_makecontext -(20000+370)
+#ifdef __NR_makecontext
+#define __SNR_makecontext __NR_makecontext
+#else
+#define __SNR_makecontext __PNR_makecontext
+#endif
+#define __PNR_freecontext -(20000+372)
+#ifdef __NR_freecontext
+#define __SNR_freecontext __NR_freecontext
+#else
+#define __SNR_freecontext __PNR_freecontext
+#endif
+#define __PNR_newuselib -(20000+420)
+#ifdef __NR_newuselib
+#define __SNR_newuselib __NR_newuselib
+#else
+#define __SNR_newuselib __PNR_newuselib
+#endif
+#define __PNR_get_mem -(20000+422)
+#ifdef __NR_get_mem
+#define __SNR_get_mem __NR_get_mem
+#else
+#define __SNR_get_mem __PNR_get_mem
+#endif
+#define __PNR_free_mem -(20000+423)
+#ifdef __NR_free_mem
+#define __SNR_free_mem __NR_free_mem
+#else
+#define __SNR_free_mem __PNR_free_mem
+#endif
+#define __PNR_clean_descriptors -(20000+424)
+#ifdef __NR_clean_descriptors
+#define __SNR_clean_descriptors __NR_clean_descriptors
+#else
+#define __SNR_clean_descriptors __PNR_clean_descriptors
+#endif
+#define __PNR_unuselib -(20000+425)
+#ifdef __NR_unuselib
+#define __SNR_unuselib __NR_unuselib
+#else
+#define __SNR_unuselib __PNR_unuselib
+#endif
+#define __PNR_unsafe_uint64_to_ptr -(20000+445)
+#ifdef __NR_unsafe_uint64_to_ptr
+#define __SNR_unsafe_uint64_to_ptr __NR_unsafe_uint64_to_ptr
+#else
+#define __SNR_unsafe_uint64_to_ptr __PNR_unsafe_uint64_to_ptr
+#endif
diff '--color=auto' -ruaN libseccomp-2.5.5/src/arch-e2k.c my_libseccomp-2.5.5/src/arch-e2k.c
--- libseccomp-2.5.5/src/arch-e2k.c	2025-05-15 12:52:21.023256104 +0300
+++ my_libseccomp-2.5.5/src/arch-e2k.c	2025-05-15 12:42:00.122753008 +0300
@@ -6,6 +6,49 @@
 #include "arch-e2k.h"
 #include "syscalls.h"
 
+ #define __SCMP_NR_BASE                  0
+
+/**
+ * Resolve a syscall name to a number
+ * @param arch the architecture definition
+ * @param name the syscall name
+ *
+ * Resolve the given syscall name to the syscall number using the syscall table.
+ * Returns the syscall number on success, including negative pseudo syscall
+ * numbers; returns __NR_SCMP_ERROR on failure.
+ *
+ */
+int e2k_syscall_resolve_name_munge(const struct arch_def *arch,
+				   const char *name)
+{
+	int sys;
+
+	/* NOTE: we don't want to modify the pseudo-syscall numbers */
+	sys = arch->syscall_resolve_name_raw(name);
+	if (sys == __NR_SCMP_ERROR || sys < 0)
+		return sys;
+
+	return (sys | __SCMP_NR_BASE);
+}
+
+/**
+ * Resolve a syscall number to a name
+ * @param arch the architecture definition
+ * @param num the syscall number
+ *
+ * Resolve the given syscall number to the syscall name using the syscall table.
+ * Returns a pointer to the syscall name string on success, including pseudo
+ * syscall names; returns NULL on failure.
+ *
+ */
+const char *e2k_syscall_resolve_num_munge(const struct arch_def *arch, int num)
+{
+	/* NOTE: we don't want to modify the pseudo-syscall numbers */
+	if (num >= 0)
+		num &= ~__SCMP_NR_BASE;
+	return arch->syscall_resolve_num_raw(num);
+}
+
 ARCH_DEF(e2k)
 
 const struct arch_def arch_def_e2k = {
@@ -13,8 +56,10 @@
 	.token_bpf = AUDIT_ARCH_E2K,
 	.size = ARCH_SIZE_64,
 	.endian = ARCH_ENDIAN_LITTLE,
-	.syscall_resolve_name = e2k_syscall_resolve_name,
-	.syscall_resolve_num = e2k_syscall_resolve_num,
+	.syscall_resolve_name = e2k_syscall_resolve_name_munge,
+	.syscall_resolve_name_raw = e2k_syscall_resolve_name,
+	.syscall_resolve_num = e2k_syscall_resolve_num_munge,
+	.syscall_resolve_num_raw = e2k_syscall_resolve_num,
 	.syscall_rewrite = NULL,
 	.rule_add = NULL,
 };
