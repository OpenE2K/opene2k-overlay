--- a/src/arch.c	2025-01-23 20:35:23.347416697 +0000
+++ b/src/arch.c	2026-03-22 17:11:50.561605428 +0000
@@ -35,6 +35,7 @@
 #include "arch-x32.h"
 #include "arch-arm.h"
 #include "arch-aarch64.h"
+#include "arch-e2k.h"
 #include "arch-loongarch64.h"
 #include "arch-m68k.h"
 #include "arch-mips.h"
@@ -65,6 +66,8 @@
 const struct arch_def *arch_def_native = &arch_def_arm;
 #elif __aarch64__
 const struct arch_def *arch_def_native = &arch_def_aarch64;
+#elif __e2k__
+const struct arch_def *arch_def_native = &arch_def_e2k;
 #elif __loongarch_lp64
 const struct arch_def *arch_def_native = &arch_def_loongarch64;
 #elif __m68k__
@@ -147,6 +150,8 @@
 		return &arch_def_arm;
 	case SCMP_ARCH_AARCH64:
 		return &arch_def_aarch64;
+	case SCMP_ARCH_E2K:
+		return &arch_def_e2k;
 	case SCMP_ARCH_LOONGARCH64:
 		return &arch_def_loongarch64;
 	case SCMP_ARCH_M68K:
@@ -207,6 +212,8 @@
 		return &arch_def_arm;
 	else if (strcmp(arch_name, "aarch64") == 0)
 		return &arch_def_aarch64;
+	else if (strcmp(arch_name, "e2k") == 0)
+		return &arch_def_e2k;
 	else if (strcmp(arch_name, "loongarch64") == 0)
 		return &arch_def_loongarch64;
 	else if (strcmp(arch_name, "m68k") == 0)
--- a/src/syscalls.h	2025-01-23 20:35:23.350750031 +0000
+++ b/src/syscalls.h	2026-03-22 17:14:02.270301499 +0000
@@ -31,6 +31,7 @@
 #include "arch-x86.h"
 #include "arch-x86.h"
 #include "arch-riscv64.h"
+#include "arch-e2k.h"
 
 /* NOTE: changes to the arch_syscall_table layout may require changes to the
  *       generate_syscalls_perf.sh and arch-syscall-validate scripts */
@@ -86,6 +87,9 @@
 
 	int sh;
 	enum scmp_kver sh_kver;
+
+	int e2k;
+	enum scmp_kver e2k_kver;
 };
 #define SYSTBL_OFFSET(NAME) offsetof(struct arch_syscall_table, NAME)
 
--- a/src/Makefile.am	2026-03-22 17:15:09.609634833 +0000
+++ b/src/Makefile.am	2026-03-22 17:15:28.479448020 +0000
@@ -32,6 +32,7 @@
 	arch-x32.h arch-x32.c \
 	arch-arm.h arch-arm.c \
 	arch-aarch64.h arch-aarch64.c \
+	arch-e2k.h arch-e2k.c \
 	arch-loongarch64.h arch-loongarch64.c \
 	arch-m68k.h arch-m68k.c \
 	arch-mips.h arch-mips.c \
--- a/src/gen_pfc.c	2025-01-23 20:35:23.347416697 +0000
+++ b/src/gen_pfc.c	2026-03-22 17:19:30.230387995 +0000
@@ -61,6 +61,8 @@
 		return "arm";
 	case SCMP_ARCH_AARCH64:
 		return "aarch64";
+	case SCMP_ARCH_E2K:
+		return "e2k";
 	case SCMP_ARCH_LOONGARCH64:
 		return "loongarch64";
 	case SCMP_ARCH_M68K:
--- a/src/arch-syscall-dump.c	2026-03-22 17:08:29.303597902 +0000
+++ b/src/arch-syscall-dump.c	2026-03-22 17:20:34.783082250 +0000
@@ -48,6 +48,7 @@
 #include "arch-s390.h"
 #include "arch-s390x.h"
 #include "arch-sh.h"
+#include "arch-e2k.h"
 
 /**
  * Print the usage information to stderr and exit
--- a/tools/util.c	2026-03-22 17:21:39.362442908 +0000
+++ b/tools/util.c	2026-03-22 17:21:34.255826798 +0000
@@ -44,6 +44,8 @@
 #define ARCH_NATIVE		AUDIT_ARCH_ARM
 #elif __aarch64__
 #define ARCH_NATIVE		AUDIT_ARCH_AARCH64
+#elif __e2k__
+#define ARCH_NATIVE		AUDIT_ARCH_E2K
 #elif __loongarch_lp64
 #define ARCH_NATIVE		AUDIT_ARCH_LOONGARCH64
 #elif __m68k__
--- a/tools/scmp_bpf_sim.c	2026-03-22 17:22:34.945225967 +0000
+++ b/tools/scmp_bpf_sim.c	2026-03-22 17:22:31.345261607 +0000
@@ -260,6 +260,8 @@
 				arch = AUDIT_ARCH_ARM;
 			else if (strcmp(optarg, "aarch64") == 0)
 				arch = AUDIT_ARCH_AARCH64;
+			else if (strcmp(optarg, "e2k") == 0)
+				arch = AUDIT_ARCH_E2K;
 			else if (strcmp(optarg, "loongarch64") == 0)
 				arch = AUDIT_ARCH_LOONGARCH64;
 			else if (strcmp(optarg, "m68k") == 0)
diff --git a/tests/regression b/tests/regression
index 5214ec7..f4d4105 100755
--- a/tests/regression
+++ b/tests/regression
@@ -51,6 +51,7 @@ GLBL_ARCH_32B_SUPPORT=" \
 GLBL_ARCH_64B_SUPPORT=" \
 	x86_64 \
 	aarch64 \
+	e2k \
 	loongarch64 \
 	mips64 \
 	parisc64 \
@@ -806,7 +807,7 @@ function run_test_live() {
 
 	# setup the arch specific return values
 	case "$arch" in
-	x86|x86_64|x32|arm|aarch64|loongarch64|m68k|parisc|parisc64|ppc|ppc64|ppc64le|ppc|s390|s390x|riscv64|sh|sheb)
+	x86|x86_64|x32|arm|aarch64|e2k|loongarch64|m68k|parisc|parisc64|ppc|ppc64|ppc64le|ppc|s390|s390x|riscv64|sh|sheb)
 		rc_kill_process=159
 		rc_kill=159
 		rc_allow=160
--- a/src/python/seccomp.pyx	2025-01-23 20:35:23.347416697 +0000
+++ b/src/python/seccomp.pyx	2026-03-22 17:25:44.483349521 +0000
@@ -206,6 +206,7 @@
     X32 - 64-bit x86 using the x32 ABI
     ARM - ARM
     AARCH64 - 64-bit ARM
+    E2K - 64-bit Elbrus
     LOONGARCH64 - 64-bit LoongArch
     M68K - 32-bit Motorola 68000
     MIPS - MIPS O32 ABI
@@ -229,6 +230,7 @@
     X32 = libseccomp.SCMP_ARCH_X32
     ARM = libseccomp.SCMP_ARCH_ARM
     AARCH64 = libseccomp.SCMP_ARCH_AARCH64
+    E2K = libseccomp.SCMP_ARCH_E2K
     LOONGARCH64 = libseccomp.SCMP_ARCH_LOONGARCH64
     M68K = libseccomp.SCMP_ARCH_M68K
     MIPS = libseccomp.SCMP_ARCH_MIPS
@@ -268,6 +270,8 @@
                 self._token = libseccomp.SCMP_ARCH_ARM
             elif arch == libseccomp.SCMP_ARCH_AARCH64:
                 self._token = libseccomp.SCMP_ARCH_AARCH64
+            elif arch == libseccomp.SCMP_ARCH_E2K:
+                self._token = libseccomp.SCMP_ARCH_E2K
             elif arch == libseccomp.SCMP_ARCH_LOONGARCH64:
                 self._token = libseccomp.SCMP_ARCH_LOONGARCH64
             elif arch == libseccomp.SCMP_ARCH_M68K:
--- a/src/python/libseccomp.pxd	2025-01-23 20:35:23.347416697 +0000
+++ b/src/python/libseccomp.pxd	2026-03-22 17:26:27.506256923 +0000
@@ -38,6 +38,7 @@
         SCMP_ARCH_X32
         SCMP_ARCH_ARM
         SCMP_ARCH_AARCH64
+        SCMP_ARCH_E2K
         SCMP_ARCH_LOONGARCH64
         SCMP_ARCH_M68K
         SCMP_ARCH_MIPS
